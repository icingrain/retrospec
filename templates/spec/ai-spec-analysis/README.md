# ai-spec-analysis Generated Job Template

Use this template when spec generates risk, migration, or business-summary analysis from Curator-confirmed evidence.

- The daemon executes the trusted `ai-analysis` capability for the current deterministic driver.
- The planning skill is `ai-spec-analysis` because it defines community candidate grouping, memory-note injection, and ambiguous-evidence handling.
- This template is the package's execution surface; the skill has no parser reference pack because it consumes Curator-confirmed evidence.
- All writes must remain under `.retrospec/spec/`.
