# sql-data-access JVM fixture

This pack complements the Pro*C pack with minimum Java/JVM data-access examples.

Use it when a generated `sql-data-access` program selects JDBC, MyBatis XML, JPA/JPQL, or Hibernate handlers. The expected evidence keeps SQL text, CRUD intent, source anchors, and ambiguous ORM/XML linkage separate.
